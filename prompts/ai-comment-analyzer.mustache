{{file}} contains comments for you. Lines containing "AI!" are instructions. If empty, guess the intent.

For each comment, follow the instructions and return a SEARCH/REPLACE block. Omit the AI comment in your response.
